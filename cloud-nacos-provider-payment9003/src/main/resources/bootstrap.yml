server:
  port: 9003

spring:
  application:
    name: cloud-nacos-provider-payment
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"